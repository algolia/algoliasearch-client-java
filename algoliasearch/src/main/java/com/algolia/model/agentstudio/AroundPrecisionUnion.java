// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost
// - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.

package com.algolia.model.agentstudio;

import com.algolia.exceptions.AlgoliaRuntimeException;
import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.core.*;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.*;
import com.fasterxml.jackson.databind.annotation.*;
import java.io.IOException;
import java.util.List;
import java.util.Map;
import java.util.logging.Logger;

/** AroundPrecisionUnion */
@JsonDeserialize(using = AroundPrecisionUnion.Deserializer.class)
public interface AroundPrecisionUnion {
  // AroundPrecisionUnion as Integer wrapper.
  static AroundPrecisionUnion of(Integer value) {
    return new IntegerWrapper(value);
  }

  // AroundPrecisionUnion as List<Map<String, Integer>> wrapper.
  static AroundPrecisionUnion of(List<Map<String, Integer>> value) {
    return new ListOfMapOfStringIntegerWrapper(value);
  }

  // AroundPrecisionUnion as Integer wrapper.
  @JsonSerialize(using = IntegerWrapper.Serializer.class)
  class IntegerWrapper implements AroundPrecisionUnion {

    private final Integer value;

    IntegerWrapper(Integer value) {
      this.value = value;
    }

    public Integer getValue() {
      return value;
    }

    static class Serializer extends JsonSerializer<IntegerWrapper> {

      @Override
      public void serialize(IntegerWrapper value, JsonGenerator gen, SerializerProvider provider) throws IOException {
        gen.writeObject(value.getValue());
      }
    }
  }

  // AroundPrecisionUnion as List<Map<String, Integer>> wrapper.
  @JsonSerialize(using = ListOfMapOfStringIntegerWrapper.Serializer.class)
  class ListOfMapOfStringIntegerWrapper implements AroundPrecisionUnion {

    private final List<Map<String, Integer>> value;

    ListOfMapOfStringIntegerWrapper(List<Map<String, Integer>> value) {
      this.value = value;
    }

    public List<Map<String, Integer>> getValue() {
      return value;
    }

    static class Serializer extends JsonSerializer<ListOfMapOfStringIntegerWrapper> {

      @Override
      public void serialize(ListOfMapOfStringIntegerWrapper value, JsonGenerator gen, SerializerProvider provider) throws IOException {
        gen.writeObject(value.getValue());
      }
    }
  }

  class Deserializer extends JsonDeserializer<AroundPrecisionUnion> {

    private static final Logger LOGGER = Logger.getLogger(Deserializer.class.getName());

    @Override
    public AroundPrecisionUnion deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException {
      JsonNode tree = jp.readValueAsTree();
      // deserialize Integer
      if (tree.isInt()) {
        try (JsonParser parser = tree.traverse(jp.getCodec())) {
          Integer value = parser.readValueAs(Integer.class);
          return new AroundPrecisionUnion.IntegerWrapper(value);
        } catch (Exception e) {
          // deserialization failed, continue
          LOGGER.finest("Failed to deserialize oneOf Integer (error: " + e.getMessage() + ") (type: Integer)");
        }
      }
      // deserialize List<Map<String, Integer>>
      if (tree.isArray()) {
        try (JsonParser parser = tree.traverse(jp.getCodec())) {
          List<Map<String, Integer>> value = parser.readValueAs(new TypeReference<List<Map<String, Integer>>>() {});
          return new AroundPrecisionUnion.ListOfMapOfStringIntegerWrapper(value);
        } catch (Exception e) {
          // deserialization failed, continue
          LOGGER.finest(
            "Failed to deserialize oneOf List<Map<String, Integer>> (error: " + e.getMessage() + ") (type: List<Map<String, Integer>>)"
          );
        }
      }
      throw new AlgoliaRuntimeException(String.format("Failed to deserialize json element: %s", tree));
    }

    /** Handle deserialization of the 'null' value. */
    @Override
    public AroundPrecisionUnion getNullValue(DeserializationContext ctxt) throws JsonMappingException {
      return null;
    }
  }
}
